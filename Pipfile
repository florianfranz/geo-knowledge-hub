[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
check-manifest = ">=0.25"

[packages]
invenio-app-rdm = {version = "~=4.0.0", extras = ["elasticsearch7", "postgresql"]}
uwsgi = ">=2.0"
uwsgitop = ">=0.11"
uwsgi-tools = ">=1.1.1"
wtforms = "==2.3.1"

[requires]
python_version = "3.8"

[pipenv]
# Needed for invenio-records>v1.5.0a1
allow_prereleases = true
